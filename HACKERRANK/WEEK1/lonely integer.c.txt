#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    int num, unique = 0;

    for(int i = 0; i < n; i++) {
        scanf("%d", &num);
        unique ^= num;
    }

    printf("%d\n", unique);
    return 0;
}
