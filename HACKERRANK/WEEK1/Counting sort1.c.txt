#include <stdio.h>

int main() {
    int n, i, x;
    int arr[100] = {0};

    scanf("%d", &n);

    for(i = 0; i < n; i++) {
        scanf("%d", &x);
        arr[x]++;
    }

    for(i = 0; i < 100; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}
